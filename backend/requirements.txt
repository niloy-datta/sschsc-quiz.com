# Core API Framework
fastapi>=0.100.0
uvicorn[standard]>=0.28.0

# Serverless Adapter — AWS Lambda / Vercel
mangum>=0.17.0

# Data Validation
pydantic>=2.0.0

# Auth
PyJWT>=2.8.0

# HTTP Client (Firestore REST + Firebase token verification)
httpx>=0.27.0

# Firestore REST service-account OAuth
google-auth>=2.29.0

# Form data support
python-multipart>=0.0.9

# Environment variables
python-dotenv>=1.0.0

# Optional — not required when using Firestore REST only (may fail on Python 3.14)
# firebase-admin>=6.2.0
