# Core API Framework
fastapi>=0.100.0
uvicorn[standard]>=0.28.0

# Serverless Adapter — Vercel / AWS Lambda / Serverless Framework
mangum>=0.17.0

# Data Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Auth / JWT
PyJWT>=2.8.0

# HTTP Client (Firestore REST + Firebase token verification)
httpx>=0.27.0

# Firestore REST service-account OAuth
# Required by backend/app/firestore.py
# Do not remove unless Firestore auth code is changed.
google-auth>=2.29.0

# Optional Firebase Admin SDK
# backend/app/main.py handles ImportError, but installing this keeps Firebase token/admin flows available.
firebase-admin>=6.2.0

# Form data support
python-multipart>=0.0.9

# Environment variables
python-dotenv>=1.0.0
